"""Agent core control-plane primitives for RepoOperator.

This package is the local-first agent proxy layer. Legacy service modules may
import these adapters during migration, but concrete work should flow through
typed actions and the action executor.
"""

